<% if (a.is_demo) { %><span class="flag flag--demo">Demo content</span><% } %><% if (a.peer_review_status === 'preprint') { %><span class="flag flag--preprint">Preprint · not peer reviewed</span><% } %><% if (a.is_periphery_pick) { %><span class="flag flag--periphery">From the periphery</span><% } %>
